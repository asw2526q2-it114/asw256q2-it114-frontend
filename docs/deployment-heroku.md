# Heroku Deployment Placeholder

Deployment is intentionally pending for this sprint implementation.

Future Heroku work should add:

- the chosen Heroku buildpack or container strategy
- runtime environment variable documentation for `NEXT_PUBLIC_API_BASE_URL`
- release commands, if the backend contract requires them
- a smoke-check URL after deploy

No Vercel configuration is included.
